"""Aether world model: latent dynamics, causal graphs, memory, autopsies.

Layer 2 of the brain. Consumes frozen perception embeddings and learns
(1) how the market state evolves (counterfactual simulation),
(2) which drivers cause which moves (living causal graph),
(3) what has been seen before and how it ended (multi-tier memory),
(4) why each trade won or lost (forensic autopsies that feed learning).
"""
