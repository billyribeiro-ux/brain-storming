"""Aether dashboard: Streamlit cockpit for monitoring, validation & feedback.

Layer 6. Read-only over the lake, checkpoints, runs, blotters, autopsies and
causal snapshots — with one write path: human feedback (data/feedback.jsonl)
that flows into the lessons buffer and thus directly into future training.
Launch: streamlit run aether/dashboard/app.py
"""
